# Environment Configuration for CI/CD
# This file documents environment variables that should be set in CI/CD systems
# to avoid the need for workflow modifications when GitHub tokens lack 'workflow' scope

# =============================================================================
# Prisma Configuration
# =============================================================================

# Disable Prisma telemetry collection
# This prevents Prisma from sending usage analytics
CHECKPOINT_TELEMETRY=0
CHECKPOINT_DISABLE=1

# Disable Prisma telemetry completely
PRISMA_DISABLE_TELEMETRY=true

# Skip automatic postinstall generation
PRISMA_SKIP_POSTINSTALL_GENERATE=true

# Set custom binary cache directory
PRISMA_QUERY_ENGINE_BINARY_CACHE_DIR=./prisma-cache

# Optional: Disable Prisma update checks
# Uncomment if you want to skip version checking
# PRISMA_SKIP_UPDATE_CHECK=true

# =============================================================================
# Next.js Configuration
# =============================================================================

# Disable Next.js telemetry collection
NEXT_TELEMETRY_DISABLED=1

# =============================================================================
# Database Configuration (for CI)
# =============================================================================

# PostgreSQL connection for testing
DATABASE_URL=postgresql://test_user:test_password@localhost:5432/test_db

# =============================================================================
# Authentication Configuration (for CI)
# =============================================================================

# NextAuth configuration for testing
NEXTAUTH_SECRET=test_secret_key_for_ci
NEXTAUTH_URL=http://localhost:3001

# =============================================================================
# Testing Configuration
# =============================================================================

# Indicate we're running in CI
CI=true

# Disable interactive prompts
NO_COLOR=1

# =============================================================================
# Performance Optimization
# =============================================================================

# Disable npm audit during CI for faster installs
NPM_CONFIG_AUDIT=false

# Use npm cache for faster installs
NPM_CONFIG_PREFER_OFFLINE=true

# =============================================================================
# GitHub Actions Specific
# =============================================================================

# These variables are automatically set by GitHub Actions
# Listed here for documentation purposes:
# GITHUB_ACTIONS=true
# GITHUB_WORKSPACE=/github/workspace
# GITHUB_REPOSITORY=owner/repo
# GITHUB_SHA=commit_sha
# GITHUB_REF=refs/heads/branch_name

# =============================================================================
# How to Use This File
# =============================================================================

# For GitHub Actions:
# Add these variables to your workflow's 'env:' section or as repository secrets

# For other CI systems:
# Export these variables in your CI configuration or pipeline scripts

# For local development:
# Copy this file to .env.local and modify as needed
# (Remember to add .env.local to .gitignore)

# Example for bash script:
# set -a && source ci.env && set +a